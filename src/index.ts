import { Name } from './models';
import { EASTERN_ORDER, WESTERN_ORDER } from './constants';
import { easternToName, mononymToName, westernToName } from './utils';

export default {
  // models
  Name,
  // constants
  EASTERN_ORDER,
  WESTERN_ORDER,
  // utils
  easternToName,
  mononymToName,
  westernToName
};
